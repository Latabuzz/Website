<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="font-sans bg-gray-50">
  <!-- Header & Navigation -->
  <header class="fixed w-full z-50 bg-white/90 backdrop-blur-sm shadow-sm">
    <div class="container mx-auto px-4 py-3 flex justify-between items-center">
      <div class="flex items-center">
        <div class="w-12 h-12 rounded-full bg-blue-500 flex items-center justify-center text-white font-bold text-xl mr-3">
          DK
        </div>
        <h1 class="text-2xl font-bold text-gray-800">
          <span class="text-blue-500">Web</span> Desa
        </h1>
      </div>
      
      <nav class="hidden md:block">
        <ul class="flex space-x-8">
          <li><a href="/" class="nav-link text-gray-700 hover:text-blue-500 font-medium">Beranda</a></li>
          <li><a href="/berita" class="nav-link text-blue-500 font-medium">Berita</a></li>
          <li><a href="/penduduk" class="nav-link text-gray-700 hover:text-blue-500 font-medium">Data Penduduk</a></li>
        </ul>
      </nav>
      
      <button class="md:hidden text-gray-700 focus:outline-none">
        <i class="fas fa-bars text-2xl"></i>
      </button>
    </div>
  </header>

  <!-- News Section -->
  <section id="news" class="pt-24 py-16 bg-white">
    <div class="container mx-auto px-4">
      <div class="text-center mb-12">
        <h2 class="text-3xl md:text-4xl font-bold text-gray-800 mb-4">
          Berita <span class="text-blue-500">Terbaru</span>
        </h2>
        <div class="w-20 h-1 bg-blue-500 mx-auto mb-6"></div>
        <p class="text-lg text-gray-600 max-w-3xl mx-auto">
          Informasi terbaru seputar kegiatan dan perkembangan di desa.
        </p>
      </div>
      
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        <% if (berita.length > 0) { %>
          <% berita.forEach(function(item) { %>
            <div class="bg-white rounded-xl shadow-lg overflow-hidden hover:shadow-xl transition-shadow duration-300">
              <div class="relative h-48 overflow-hidden">
                <img src="https://images.unsplash.com/photo-1586339392-d1a8af8b7ee1?ixlib=rb-4.0.3&auto=format&fit=crop&w=1470&q=80" 
                     alt="<%= item.judul %>" 
                     class="w-full h-full object-cover">
                <div class="absolute top-4 left-4 bg-blue-500 text-white px-3 py-1 rounded-full text-sm font-medium">
                  Berita
                </div>
              </div>
              <div class="p-6">
                <div class="flex items-center text-gray-500 text-sm mb-3">
                  <i class="far fa-calendar-alt mr-2"></i> 
                  <%= new Date(item.created_at).toLocaleDateString('id-ID') %>
                </div>
                <h3 class="text-xl font-bold text-gray-800 mb-3"><%= item.judul %></h3>
                <p class="text-gray-600 mb-4">
                  <%= item.konten.length > 100 ? item.konten.substring(0, 100) + '...' : item.konten %>
                </p>
                <div class="flex justify-between items-center">
                  <small class="text-gray-500">
                    <i class="fas fa-user mr-1"></i><%= item.penulis %>
                  </small>
                  <a href="#" class="text-blue-500 font-medium hover:text-blue-600 flex items-center">
                    Baca Selengkapnya <i class="fas fa-arrow-right ml-2"></i>
                  </a>
                </div>
              </div>
            </div>
          <% }); %>
        <% } else { %>
          <div class="col-span-full">
            <div class="bg-blue-50 border border-blue-200 rounded-xl p-8 text-center">
              <i class="fas fa-newspaper text-blue-500 text-5xl mb-4"></i>
              <h3 class="text-xl font-bold text-gray-800 mb-2">Belum ada berita</h3>
              <p class="text-gray-600">Berita akan ditampilkan di sini setelah ditambahkan.</p>
            </div>
          </div>
        <% } %>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer class="bg-gray-900 text-white py-12">
    <div class="container mx-auto px-6">
      <div class="text-center">
        <div class="flex items-center justify-center mb-6">
          <div class="w-12 h-12 rounded-full bg-blue-600 flex items-center justify-center text-white font-bold text-xl mr-3">
            DK
          </div>
          <div>
            <h3 class="text-xl font-bold">Web Desa</h3>
            <p class="text-gray-400 text-sm">Portal Informasi Desa</p>
          </div>
        </div>
        <div class="border-t border-gray-800 pt-6">
          <p class="text-gray-500 text-sm">
            © 2024 Web Desa. Dibuat dengan ❤️ untuk melayani warga.
          </p>
        </div>
      </div>
    </div>
  </footer>
</body>
</html>
